version: "3.8"
services:
  node:
    image: node:14.17.0
    container_name: gravy-node-build
    working_dir: /usr/src/app
    volumes:
      - ../../.:/usr/src/app
    command: sh -c "yarn install && yarn react-scripts build"
